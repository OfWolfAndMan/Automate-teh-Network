---

#tasks:
# - name: Include Vars
   #include_vars: group_vars/Network-Physical.yml

- name: GENERATE CONFIGS FOR EACH OS
  template:
    src: "templates/Network-Physical.j2"
    dest: "../configs//{{ inventory_hostname }}.cfg"
